package CoreJavaTest;

public class hitachiPack {
/*
    public static void main(String[] args) {

       List<Integer> num = Arrays.asList(1,2,3,4,2,6,6,9,2,22,2,2,4,5,6,5,5,5);
        Map<Integer, Long> mapval = num.stream()
                                        .collect(Collectors.groupingBy(
                                                Function.identity(), Collectors.counting()
                                        ));
        System.out.println(mapval);


    }

 */
/*
    private static int[] removeArray(int[] numbers) {
        int intarrLen = numbers.length;
        int[] temp = new int[intarrLen];
        int curInc = 0;
        if(intarrLen>0){
            int j=0;

            for (int i=0; i<intarrLen-1; i++){
                if(numbers[i]!=numbers[i+1]){
                    temp[j++] = numbers[i];
                    curInc++;
                }
            }
        }
        int[] curArr =
        return temp;
    }
*/
}
